# 隐私政策

**生效日期：2026 年 9 月 17 日**
**适用版本：V4.0.2 及后续版本**

---

**本应用不联网、没有服务器、不收集任何个人信息，也不会向任何第三方应用注入操作。**
本应用运行中产生的一切内容（关键词、日志）都只保存在你自己的设备里。

---

## 一、我们收集哪些信息

**答：不收集。**

| 项目 | 是否收集 | 说明 |
|---|---|---|
| 账号、手机号、邮箱 | ❌ 不收集 | 应用内没有注册 / 登录功能 |
| 通讯录、短信、通话记录 | ❌ 不收集 | 未申请相关权限 |
| 位置信息 | ❌ 不收集 | 未申请定位权限 |
| 设备标识符（IMEI / MEID / MAC / OAID / Android ID / 序列号） | ❌ 不收集 | **未申请 `READ_PHONE_STATE` 等任何设备信息权限，技术上无法获取** |
| 应用安装列表 | ❌ 不收集 | 未申请 `QUERY_ALL_PACKAGES` 等权限 |
| 照片、文件、媒体库 | ❌ 不收集 | 仅在你主动点「导出日志」时，向设备本地写入日志文件 |
| 相机、麦克风 | ❌ 不使用 | 未申请 `CAMERA` / `RECORD_AUDIO` 权限 |
| 屏幕画面内容 | ⚠️ **仅本地处理** | 画面只在设备内存中识别，识别结果不保存、不上传（详见第六节） |
| 运行日志 | 💾 **仅本地存储** | 存在应用私有目录，可查看、可导出、可随时删除 |

本应用**没有集成**任何第三方统计、广告、支付、推送或崩溃上报 SDK。

---

## 二、关于「不联网」的技术性承诺

本应用在 `AndroidManifest.xml` 中**没有声明 `android.permission.INTERNET` 权限**。

这意味着：**Android 系统层面就不允许本应用发起任何网络请求**（包括 HTTP、HTTPS、TCP、UDP）。
任何人都可以自行验证（见第九节）。

具体声明：

1. **无自建服务器**：本项目没有任何后端服务、没有域名、没有 API 接口。
2. **无数据上传**：不存在任何形式的数据回传、埋点、遥测。
3. **无账号体系**：没有服务器，自然没有账号，也就没有账号数据。
4. **无需联网即可完整使用**：所有功能（识别、触发、清理、日志）均在本机完成。

> 唯一与"外部"有关的例外：文字识别（OCR）能力由设备上的 **Google Play services（ML Kit）**
> **在设备本地**提供并完成计算。模型文件的提供与更新由 Google Play services 负责，
> 其网络行为遵循 Google 的隐私政策，与本项目无关；本应用既不参与、也不接收、更不上传任何数据。
> 若你的设备没有 Google Play services，应用会自动改用系统无障碍服务的节点文字读取，仍然不联网。

---

## 三、应用申请的全部权限（完整清单）

本应用仅申请以下权限，无其他：

| 权限 | 用途 | 是否必需 |
|---|---|---|
| `SYSTEM_ALERT_WINDOW` | 显示悬浮控制面板 | 必需 |
| `FOREGROUND_SERVICE` | 保持后台监测 | 必需 |
| `FOREGROUND_SERVICE_MEDIA_PROJECTION` | 前台服务类型（Android 9/10 屏幕采集） | 系统要求 |
| `FOREGROUND_SERVICE_SPECIAL_USE` | 前台服务类型（Android 14+ 无障碍截图） | 系统要求 |
| `POST_NOTIFICATIONS` | 显示"正在运行"的常驻通知 | Android 13+ 建议 |
| `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` | 申请加入电池优化白名单 | 建议 |
| `WRITE_EXTERNAL_STORAGE`（仅限 Android 9，maxSdk 28） | 写入运行日志 | 仅旧系统 |

**本应用明确未申请**：`INTERNET`、`ACCESS_NETWORK_STATE`、`READ_PHONE_STATE`、
定位类权限、`CAMERA`、`RECORD_AUDIO`、通讯录 / 短信 / 通话记录类权限、
`QUERY_ALL_PACKAGES`、`PACKAGE_USAGE_STATS`。

---

## 四、无障碍权限专项说明

无障碍权限是本应用唯一敏感的权限，单独说明。

### 4.1 我们用它做了三件事

1. **`GLOBAL_ACTION_HOME`** —— 相当于你在设备上按了一次「主页键」。
2. **`GLOBAL_ACTION_RECENTS`** —— 相当于你上滑打开了一次「最近任务」界面。
3. **查找并点击系统「清除全部」按钮** —— 相当于你在最近任务界面点了一下系统自己的清理按钮。

此外，在部分机型上会用无障碍读取**当前窗口的控件文字**，作为文字识别的补充手段
（读取后立即在本机比对，不保存、不上传）。

### 4.2 我们明确不做的事

- **不向任何第三方应用注入点击、滑动、长按、按键。**
  本应用的无障碍手势（`dispatchGesture`）**只**用于点击系统最近任务界面上的清除按钮，
  且**仅在**你主动开启「备用清理按钮」后才会使用（默认关闭）。
- **不代替用户操作任何应用。** 触发后的动作是"退出到桌面并清理后台"，
  这是在**结束**当前应用的运行，而不是在应用内部替用户执行任何操作。
- **不读取、不修改任何应用的内存或数据**，不做进程注入、HOOK、调试附加。

### 4.3 授权机制说明

无障碍服务（AccessibilityService）是 **Android 官方为残障人士及自动化辅助场景提供的公开 API**。
任何应用声明该服务时：

- 必须由用户在**系统设置中手动开启**（应用无法自行开启）；
- 必须在系统无障碍设置页面**明示用途**（本应用填写的说明为：
  "需要无障碍服务来回到系统主屏幕并清理后台任务"）；
- 用户可**随时在系统设置中关闭**。

---

## 五、与「外挂 / 作弊工具」的界限

我们用**可验证的技术事实**逐条对照：

| 外挂 / 作弊程序的典型行为 | 本软件 | 判定 |
|---|---|---|
| 读写、修改其他进程的内存数据 | **不读取、不写入、不修改任何进程内存**；未使用 `ptrace`、未读写 `/proc/*/mem`、未做进程注入 | 不具备 |
| 注入动态库、HOOK 函数（Xposed / Frida / Inline Hook / PLT Hook 等） | **无任何注入或 HOOK 行为**，未集成任何注入框架 | 不具备 |
| 拦截、篡改、伪造网络协议包 | **不联网**（未声明 `INTERNET` 权限），未使用 `VpnService`、未设置代理 | 不具备 |
| 在第三方应用内部代替用户操作（自动点击、脚本连点、自动执行任务） | **不向任何第三方应用注入触摸或按键** | 不具备 |
| 修改 / 替换客户端文件、资源、存档、配置 | 不读写任何其他应用的私有数据 | 不具备 |
| 针对特定进程进行附加、调试、监控 | 只通过系统接口获取**整屏画面**，**不枚举、不附加、不监控任何进程** | 不具备 |
| 加速、变速、修改其他应用的时间或运行逻辑 | 无此类功能 | 不具备 |
| 获取不正当的应用内利益 | 触发后的动作是**退出到系统桌面并清理后台**，反而**终止**当前应用的运行，不产生任何应用内收益 | 不具备 |

### 5.1 本软件的技术本质

本软件由三个**系统级**动作构成：

> **看屏幕**（系统屏幕采集 / 无障碍截图）
> **→ 本地识别**（设备端 OCR / 窗口文字）
> **→ 回桌面 + 清后台**（系统级操作）

其中"回桌面"和"清后台"等价于用户**自己按主页键、再划掉后台**，
属于每个用户都能手动完成的系统操作，不涉及任何应用的内部逻辑。

### 5.2 与相关法律条文的对应关系

| 法律 / 规定 | 规制的行为 | 本软件的对应情况 |
|---|---|---|
| 《中华人民共和国刑法》第 285 条第 2 款（非法获取计算机信息系统数据罪） | 侵入系统并非法获取其中存储、处理、传输的数据 | 本软件不读取任何应用的内部数据，仅获取系统渲染的屏幕画面 |
| 《中华人民共和国刑法》第 286 条（破坏计算机信息系统罪） | 对系统功能进行删除、修改、增加、干扰 | 本软件不修改任何程序或数据 |
| 《刑法》第 285 条第 3 款及两高《关于办理危害计算机信息系统安全刑事案件应用法律若干问题的解释》 | 提供专门用于侵入、非法控制计算机信息系统的程序、工具 | 本软件所调用的均为 Android 官方公开 API，不属于此类工具 |
| 《计算机软件保护条例》第 24 条 | 未经许可**修改**、翻译他人软件 | 本软件不修改任何其他软件的代码、资源或数据 |
| 《中华人民共和国网络安全法》第 41、42 条 | 收集、使用个人信息应明示同意；不得泄露、篡改、毁损 | 本软件不收集任何个人信息 |
| 《中华人民共和国个人信息保护法》第 5、6、13 条 | 处理个人信息应合法、正当、必要、诚信，限于最小范围 | 本软件不处理个人信息 |
| 《移动互联网应用程序信息服务管理规定》 | 规范 App 信息服务与个人信息处理活动 | 本软件不联网、无信息服务、不处理个人信息 |
| 《App 违法违规收集使用个人信息行为认定方法》 | 认定各类违规收集使用个人信息的行为 | 本软件不存在任一列举情形 |
| GB/T 35273-2020《信息安全技术 个人信息安全规范》 | 个人信息处理各环节的安全要求 | 本软件不处理个人信息，不涉及 |

> 以上为本项目的理解与说明，不构成法律意见。若你对某一具体使用场景的合规性存疑，
> 请咨询专业法律人士。

---

## 六、屏幕画面与文字识别的说明

- 画面获取依赖 Android 官方接口（Android 14+ 使用无障碍截图；Android 9/10 使用系统屏幕采集，
  会在状态栏显示采集图标并需要你一次性确认）。
- 画面数据**只在设备内存中短暂存在**，用于计算与识别，**不会被保存为图片或视频**，
  也不会离开设备。
- 如果某个应用设置了**禁止截屏**（FLAG_SECURE），系统将只返回黑屏，本软件无法获取其中内容
  —— 这属于 Android 的安全机制，本软件无法绕过，也**不会尝试绕过**。

---

## 七、数据存储与删除

- 日志、触发记录、你的偏好设置均保存在**应用私有目录**。
- 卸载应用会一并清除这些数据。
- 你也可以随时在「更多 → 导出到下载」备份，或直接删除文件。

---

## 八、未成年人使用

本应用不收集任何信息，也不存在诱导或不适内容。未成年人请在监护人指导下使用。

---

## 九、自行验证

我们欢迎审计，并且这些验证都不需要信任我们的说法：

1. **检查权限**：查看 `AndroidManifest.xml`（或反编译 APK），确认没有 `INTERNET` 权限、
   没有设备信息相关权限。
2. **抓包验证**：用任意抓包工具监测本应用的流量，结果应为空。
3. **系统指示器**：Android 14+ 在使用屏幕采集时会显示系统指示器，你可在状态栏看到。

---

## 十、政策更新

若未来功能发生重大变化（例如引入任何需要联网的能力），本政策会同步更新，
并在 GitHub 仓库中保留历史版本。继续使用即视为接受更新后的政策。

## 十一、联系方式

请通过本项目 GitHub 仓库的 **Issues** 或**频道**提交问题与建议。
